#!/usr/bin/env bash

set -euo pipefail

source "$(cd "$(dirname "$0")" && pwd)/_workspace_common.sh"

ensure_docker

docker build --pull -t "$IMAGE_NAME:latest" "$WORKSPACE_ROOT/templates/workspace"
echo "rebuilt workspace image: $IMAGE_NAME:latest"
